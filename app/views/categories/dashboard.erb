<h1>Categories</h1>

<% @categories.each do |category| %>
  <h5>Category: <%= category.name %>, Average Price: $<%= '%.2f' % category.average_item_price %></h5>
<% end %>

<h3>Category with Most Expensive Item: <%= Category.most_expensive_item.name %></h3>
<h3>Category with Least Expensive Item: <%= Category.least_expensive_item.name %></h3>
