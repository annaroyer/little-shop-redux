<head>
  <link rel="stylesheet" type="text/css" href="../../../reset.css">
  <link rel="stylesheet" type="text/css" href="../../../main.css">
  <title>Don't Enter : Shop Inside</title>
  <link href="/images/favicon.ico" rel="icon" type="image/x-icon" />
  <link href="https://fonts.googleapis.com/css?family=Fredericka+the+Great|Josefin+Slab" rel="stylesheet">
</head>

<header class="top">
  <figure class="banner">
    <a href="/"><img src="../../../images/little_shop_rusty50.png"/></a>
  </figure>
</header>

<nav class="nav">
  <section class="dropdown">
    <a href="/merchants"><button class="dropbutton">Merchants</button></a>
    <div class="dropdown-content">
      <a href="/merchants-dashboard">Merchants Dashboard</a>
      <a href="/merchants/new">Create New Merchant</a>
      <form action="/merchants?:params" method="GET">
        <input class="find" type="text" name="name" placeholder="Find by Name...">
      </form>
    </div>
  </section>

  <section class="dropdown">
    <a href="/categories"><button class="dropbutton">Categories</button></a>
    <div class="dropdown-content">
      <a href="/categories-dashboard">Categories Dashboard</a>
      <a href="/categories/new">Create New Category</a>
      <form action="/categories?:params" method="GET">
        <input class="find" type="text" name="name" placeholder="Find by Name...">
      </form>
    </div>
  </section>

  <section class="dropdown">
    <a href="/items"><button class="dropbutton">Items</button></a>
    <div class="dropdown-content">
      <a href="/items-dashboard">Items Dashboard</a>
      <a href="/items/new">Create New Item</a>
      <form action="/items?:params" method="GET">
        <input class="find" type="text" name="title" placeholder="Find by Name...">
      </form>
    </div>
  </section>
</nav>

<h1 class="page-title">Items Info</h1>
<section class="container">
  <p>Total Items: <%= @items.count %></p>
  <p>Average Price Per Item: $<%= "%.2f" % @items.average_price %></p>
  <section class="items">
    <figure class="featured-item">
      <p>Most Recently Created Item:</p><br />
      <p><%= @items.most_recently_created.title %></p>
      <a href="/items/<%= @items.most_recently_created.id %>">
        <img class="item-image" src="<%= @items.most_recently_created.image %>" alt="<%= @items.most_recently_created.title %>">
      </a>
      <center><p>$<%= "%.2f" % @items.most_recently_created.price %></p></center>
      <center><p>Created <%= @items.most_recently_created.created_at.strftime("%B %d, %C") %></p></center>
    </figure>
    <figure class="single-item">
      <p>Oldest Item:</p><br />
      <p> <%= @items.oldest.title %></p>
      <a href="/items/<%= @items.oldest.id %>">
        <img class="item-image" src="<%= @items.oldest.image %>" alt="<%= @items.oldest.title %>">
      </a>
      <center><p>$<%= "%.2f" % @items.oldest.price %></p></center>
      <center><p>Created <%= @items.oldest.created_at.strftime("%B %d, %C") %></p></center>
    </figure>
  </section>
</section>
